alert ("Bienvenido, porfavor llene los datos consiguientes");

var nombre = prompt ("ingrese su nombre y apellido");

var edad = parseInt(prompt ("ingrese su edad"));

var numeroDePrueba = parseInt(prompt("ingrese número de prueba"));

if (edad >= 18) {
    alert ("Hola " + nombre)}
     else { 
        alert ("usted es menor de edad, regrese en unos años")

        };

if ((numeroDePrueba >=10) && (numeroDePrueba <= 50)) {
    alert ("genial!")}
        else {
            alert ("Señor, usted es diabólico")
        };
